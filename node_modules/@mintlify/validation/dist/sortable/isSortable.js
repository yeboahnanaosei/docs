import { SORT_DIRECTION } from '@mintlify/models';
export function isSortable(obj) {
    return (obj != undefined &&
        obj.options != undefined &&
        obj.options.direction != undefined &&
        obj.options.direction in SORT_DIRECTION);
}
